var mysql = require("mysql");
function connectServer() {
    var connection = mysql.createConnection({
        host: 'localhost',
        port: '3306',
        database: 'spdsCommand',
        user: 'root',
        password: 'whr1997'
    });
    return connection;
}


function selectFun(client, devices_id, callback) {
    //client为一个mysql连接对象
    var sqlselect = 'select * from datastream'+devices_id+' order by update_at desc limit 1;';
    console.log(sqlselect);
    client.query(sqlselect, function(err, results) {
        if (err) {
            console.log('[SELECT ERROR] - ', err.message);
            return err;
        }
        callback(results);
    });
}

exports.connectDatadb = connectServer;
exports.selectdata = selectFun;